hid520220 = {

		click=function(player)
			

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid520221 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520222 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid520223 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid520224 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid520225 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid520320 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520321 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520322 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520323 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520324 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520325 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520420 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520421 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520422 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520424 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid520425 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerleftbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}


















hid521220 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521221 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521222 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521223 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid521224 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid521225 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521320 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521321 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521322 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521323 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521324 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521325 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}

hid521420 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid521421 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid521422 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid521423 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid521424 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}
hid521425 = {

		click=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end,
			
		click2=function(player)

			if(player.registry["constable"]>0 or player.gmLevel==99) then
					return
			end


			if(player.mapRegistry["ownerrightbooth"]~=player.ID) then
				player:warp(52,math.random(6,10),math.random(10,15))
				player:sendMinitext("This booth does not belong to you!")
			end
		end

}